{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": "./build",
    "noEmit": true,
    // all other types are not needed here
    "types": [
      "node"
    ]
  },
  "include": [
    "build/**/*.ts",
    "build/**/*.mts",
  ]
}
